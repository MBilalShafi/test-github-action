name: Run a workflow every time a comment is dropped on an issue

on:
  issues:
    types: [closed]

jobs:
  add-tag:
    runs-on: ubuntu-latest
    
    steps:
      - name: "log stuff"
        run: echo '${{ toJSON(github) }}' | jq
        shell: bash
